pipeline{
    agent any

    environment{
        IMAGE_NAME = "danush242211ay/devops:${GIT_COMMIT}"
    }
    stages{
        stage('Git-Checkout'){
            steps{
                git url : 'https://github.com/danush242211ay/devops.git' ,branch : 'main'
            }
        }
        stage('Build'){
            steps{
                sh '''
                printenv
                pwd
                docker build -t ${IMAGE_NAME} .
                '''
            }
        }
    }
}